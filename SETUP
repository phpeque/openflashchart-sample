Global setup:

 Set up git
  git config --global user.name "Taufek Johar"
  git config --global user.email phpeque@gmail.com
        

Next steps:

  mkdir openflashchart-sample
  cd openflashchart-sample
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:phpeque/openflashchart-sample.git OR https://phpeque@github.com/phpeque/openflashchart-sample.git
  git push -u origin master
      
